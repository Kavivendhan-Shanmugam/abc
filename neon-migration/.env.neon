# Neon PostgreSQL Database Configuration
# Replace with your actual Neon database URL
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require

# JWT Configuration (generate a strong secret)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Configuration (for local development)
PORT=3002

# Frontend Configuration (update with your Netlify URL after deployment)
VITE_API_URL=https://your-app-name.netlify.app

# Optional: For local PostgreSQL development
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your_local_postgres_password
# DB_NAME=cyber_security_leave_portal
# DB_SSL=false

# Migration Configuration (for running the migration script)
# OLD_DB_HOST=your-mysql-host
# OLD_DB_USER=your-mysql-username
# OLD_DB_PASSWORD=your-mysql-password
# OLD_DB_NAME=cyber_security_leave_portal
